class Solution {
    func arrayPairSum(_ nums: [Int]) -> Int {
        var sum = 0
        
        var descSortedSum = nums.sorted(by: <)
        
        for i in 0..<descSortedSum.count where i%2==0{
            sum += descSortedSum[i]
        }
        return sum
    }
}